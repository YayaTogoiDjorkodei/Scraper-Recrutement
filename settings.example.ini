; Copy this file to settings.local.ini and enter your own GeoNames username.
; Local settings and IP_proxies.txt must never be committed.
[geonames]
username =

[paths]
; Existing ip:port:username:password format is unchanged.
proxy_file = IP_proxies.txt
